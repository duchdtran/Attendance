<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/group_meeting_session"
        android:visible="false">

        <item android:id="@+id/option_search"
            android:title="Tìm kiếm"
            android:icon="@drawable/ic_search"
            app:showAsAction="collapseActionView|ifRoom"
            app:actionViewClass="androidx.appcompat.widget.SearchView" />
        <item
            android:id="@+id/option_filter"
            android:title="Lọc"
            android:icon="@drawable/ic_filter"
            app:showAsAction="always"/>
    </group>

    <group
        android:id="@+id/group_detail_meeting"
        android:visible="false">
        <item android:id="@+id/option_search_meeting"
            android:title="Tìm kiếm"
            android:icon="@drawable/ic_search"
            app:showAsAction="collapseActionView|ifRoom"
            app:actionViewClass="androidx.appcompat.widget.SearchView" />
        <item android:id="@+id/option_attendee_meeting"
            app:showAsAction="always"
            android:title="Người tham dự"
            android:icon="@drawable/ic_participants"/>
    </group>
    <group
        android:id="@+id/group_detail_session"
        android:visible="false">

        <item android:id="@+id/option_scan"
            app:showAsAction="always"
            android:title="Scan QR Code"
            android:icon="@drawable/ic_scan"/>
        <item android:id="@+id/option_attendee_session"
            app:showAsAction="always"
            android:title="Người tham dự"
            android:icon="@drawable/ic_participants"/>
    </group>
</menu>